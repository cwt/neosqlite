"""
Test suite for Tier-1 (SQL) optimization correctness.

These tests verify that Tier-1 (SQL) implementations produce identical results
to Tier-3 (Python fallback) when the kill switch is enabled.

Location: tests/test_tier1/
- Follows the same pattern as tests/test_tier2/ for Tier-2 tests
"""
